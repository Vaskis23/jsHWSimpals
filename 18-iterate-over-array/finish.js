/** ЗАДАЧА 18 - Перебор элементов массива
 *
 * 1. Создайте массив с несколькими элементами
 *
 * 2. Используя один из методов массивов, переберите все элементы
 * и выведите каждый элемент в консоль
 */

const arr = [1, 2, 3, 4];
arr.forEach((element) => {
    console.log(element);
});

//использовал метод forEach() для перебора каждого элемента массива и вывода 
//использовал стрелочную функцию для вывода каждого элемента